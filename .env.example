# Copy to .env and fill values
PORT=3000

# REQUIRED: Set a strong admin password
ADMIN_PASSWORD=your-secure-password-here

# Optional: Twilio credentials to send real SMS
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM=+1234567890

# In production use a persistent store (Redis/Database) instead of in-memory Map
